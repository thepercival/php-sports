Sports\Place:
  exclusion_policy: ALL
  exclude: false
  properties:
    id:
      type: integer
      groups: [structure]
    extraPoints:
      type: integer
      groups: [structure]
    qualifiedPlace:
      type: SportsHelpers\PlaceLocation
      groups: [structure]
      read_only: true
    placeNr:
      type: integer