Sports\Competition\Referee:
  exclusion_policy: ALL
  exclude: false

  properties:
    priority:
      type: integer
    initials:
      type: string
    name:
      type: string
    emailaddress:
      type: string
      groups: [privacy]
    userId:
      type: number
    info:
      type: string

