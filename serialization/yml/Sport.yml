Sports\Sport:
  exclusion_policy: ALL
  exclude: false
  properties:
    name:
      type: string
      groups: [noReference]
    team:
      type: boolean
      groups: [noReference]
    customId:
      type: integer
      groups: [noReference]
    defaultGameMode:
      groups: [noReference]
    defaultNrOfSidePlaces:
      type: integer
      groups: [ noReference ]