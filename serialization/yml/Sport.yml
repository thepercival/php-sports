Sports\Sport:
  exclusion_policy: ALL
  exclude: false
  properties:
    name:
      type: string
      groups: [noReference]
    team:
      type: boolean
      groups: [noReference]
    customId:
      type: integer
      groups: [noReference]
    defaultGameMode:
      type: int
      access_type: public_method
      accessor:
        getter: getDefaultGameModeNative
        setter: setDefaultGameModeNative
      groups: [noReference]
    defaultNrOfSidePlaces:
      type: integer
      groups: [ noReference ]