Sports\Sport:
  exclusion_policy: ALL
  exclude: false
  properties:
    name:
      type: string
      groups: [noReference]
    team:
      type: boolean
      groups: [noReference]
    customId:
      type: integer
      groups: [noReference]
    gameMode:
      type: integer
      groups: [noReference]